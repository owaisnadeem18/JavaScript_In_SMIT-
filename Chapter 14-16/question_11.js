// Write a program to initialize an array with city names, copy 3 array elements from cities array to selected Cities array.

let arr = ["Karachi", "Lahore", "Islamabad", "Faisalabad", "Multan"];

selected_citites = arr.slice(0, 3);

document.write("The list of the cities is = ", arr, "</br>");
document.write("The list of selected citites is = ", selected_citites);
