// find whether the string entered is palindrome or not

let str = "Madam";

str = str.split("");
if (str[0] == str[str.length - 1] && str[1] == str[str.length - 2]) {
  console.log("The string is palindrome");
} else {
  console.log("The string is palindrome");
}
