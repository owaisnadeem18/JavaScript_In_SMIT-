// Write a program that checks whether the given input is an even number or an odd number.

let a_number = prompt("Enter a number = ");

if (a_number % 2 == 0) {
  document.write("The number " + a_number + " is an even number");
} else {
  document.write("The number " + a_number + " is an odd number");
}
