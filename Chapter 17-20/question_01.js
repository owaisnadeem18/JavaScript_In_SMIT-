// Declare and initialize an empty multidimensional array. (Array of arrays)

let arr = [[], [], []]; // This is a multi dimensional array which is empty

console.log(arr);
console.log(typeof arr);
